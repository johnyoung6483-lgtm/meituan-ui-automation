---
name: meituan-ui-automation
description: 美团 APP UI 自动化技能。通过 Android 无障碍服务（Accessibility Service）打开美团，自动搜索指定关键词（如"肯德基""奶茶"）并筛选商家，自动把指定商品加入购物车，最终停留在"去结算"页面并强制弹窗提醒用户，由用户人工按指纹/密码确认支付；全程绝不点击任何"支付/付款"确认按钮，并内置广告弹窗跳过与定位漂移回退逻辑。当用户请求"打开美团搜 XX""把 XX 加进美团购物车""美团加购先别付款""帮我选好美团订单等到结算页"等场景时使用。
---

# 美团 UI 自动化（Meituan UI Automation）

面向 Operit（AAswordman/Operit，手机端 AI 助手）及其 Agent（大猫猫）封装的标准技能包。
提供 **两种执行模式**，共用同一套安全护栏：

- **方式 A（推荐）**：Agent 使用 Operit 内置的无障碍通道按下方步骤表执行；
- **方式 B（脚本）**：在装有 atx-agent 的设备上用 `uiautomator2` 直跑 Python 状态机。

## 一、安全红线（最高优先级，任何模式下不可违反）

1. **绝不点击支付确认按钮。** 任何文本命中 `scripts/safety_core.py` 中 `DEFAULT_PAYMENT_SUBSTRINGS` / `DEFAULT_PAYMENT_PATTERNS`（如：确认支付、立即支付、确认付款、去支付、提交订单、确认下单……）的节点，一律禁止点击；每次点击前必须经过 `guard_click()`。
2. **到达"去结算"页即停。** 打完购物车并点击"去结算"进入结算页后，自动化立即停止，**不再点击任何按钮**，只被动停留。
3. **付款前强制提醒。** 进入结算页后必须立即触发提醒弹窗/横幅（系统通知 + 桌面提示），明确提示"请人工核对订单并手动完成支付（指纹/密码）"，并等待用户本人操作。
4. **等支付阶段只读。** 等待/监控期间只允许读取屏幕文本判断"支付成功"标志，禁止任何点击、上滑、提交类动作。
5. **防御先行。** 即使识别逻辑误判，也不得用"尝试一点再撤销"的方式逼近支付按钮——保持"宁可停止，不可代付"。

## 二、触发场景（示例）

- "帮我打开美团，搜肯德基，把香辣鸡腿堡加到购物车"
- "美团上给我找家奶茶店，加三杯杨枝甘露到购物车，先不要付款"
- "把订单选到去结算那一步，等我按指纹"

## 三、前置条件

| 项目 | 说明 |
| --- | --- |
| 设备 | Android 手机，已安装美团 APP（包名 `com.sankuai.meituan`） |
| 方式 A | Operit 已开启"无障碍服务"授权（设置 → 无障碍 → Operit） |
| 方式 B | 开启 USB 调试；`pip install -U uiautomator2`；`python -m uiautomator2 init` 部署 atx-agent |

## 四、一键调用

### 方式 A：Agent 无障碍通道执行步骤表

每个步骤都遵循：**查找 → 校验（analyze_screen / 护栏）→ 动作 → 页面体检**。

| # | 目标 | 查找规则（默认文案，可在 config 覆盖） | 动作 | 校验 |
| --- | --- | --- | --- | --- |
| 1 | 打开美团 | 包名 `com.sankuai.meituan` | open_app | 出现搜索框（含"搜索"文案/hint） |
| 2 | 搜索关键词 | 搜索框 hint/文案含"搜索" | 输入关键词（如 肯德基）→ 点"搜索"/回车 | 出现结果列表或"商家"Tab |
| 3 | 筛选商家 | Tab/文案"商家" | 点击"商家" | 出现商家卡片 |
| 4 | 进入商家 | 卡片文案包含商家名（默认=关键词） | 点击该商家卡片 | 出现商品列表/点菜区 |
| 5 | 加购商品 | 文案包含商品名 | 点击商品行 → 若有"选规格"则选规格并"确定" → 点"加入购物车" | 出现"已加入购物车"或购物袋角标变化 |
| 6 | 打开购物车 | 底部/文案"购物车" | 点击"购物车" | 出现"去结算"按钮 |
| 7 | 去结算页 | 文案"去结算" | 点击"去结算"（**本流程最后一个点击动作**） | 出现"提交订单/订单金额/收银台/支付方式"等结算页标志 |
| 8 | 强制提醒并停止 | — | 触发付款前提醒（通知+横幅）；可选被动等待 | 状态码 42 |

> 每一步前都要用 `python3 scripts/meituan_automation.py --analyze` 拿到当前页面体检（是否含支付按钮 / 广告弹窗 / 定位异常），再决定动作；**绝不凭猜测点击**。

### 方式 B：脚本一行调用

```bash
# 默认流程：搜"肯德基"，进店，加购"香辣鸡腿堡"，停在去结算页并等待 10 分钟人工支付
python3 scripts/meituan_automation.py --keyword 肯德基 --product 香辣鸡腿堡 --wait-user 10

# 多商品 / 带规格
python3 scripts/meituan_automation.py --keyword 奶茶 --merchant "一点点" \
  --product "杨枝甘露,波霸奶茶" --spec 中杯

# 只读体检（不执行任何动作，Agent 决策用）
python3 scripts/meituan_automation.py --analyze

# 预览动作清单（不连设备）
python3 scripts/meituan_automation.py --keyword 肯德基 --product 香辣鸡腿堡 --plan
```

### 参数表

| 参数 | 说明 | 默认 |
| --- | --- | --- |
| `--keyword` | 搜索关键词 | 肯德基 |
| `--merchant` | 目标商家名（默认取关键词） | 同 keyword |
| `--product` | 商品名，多个用英文逗号分隔 | 必填 |
| `--spec` | 规格（如"中杯"），可选 | 无 |
| `--wait-user` | 停在结算页后被动等待分钟数（0=立即退出提醒） | 10 |
| `--device` | 后端：`u2`（uiautomator2）/ `plan` | u2 |
| `--addr` | u2 设备地址（usb 或 ip:port） | usb |
| `--config` | 配置文件路径 | config/skill_config.yaml |
| `--analyze` | 只输出当前页面体检 JSON | 关 |
| `--plan` | 只打印动作清单，不连设备 | 关 |

## 五、广告弹窗与定位漂移容错

- **广告/营销弹窗**：识别特征词（红包、领券、抽奖、广告、推广…）后，只点击"关闭/跳过/×/我知道了"等关闭类按钮；每环节最多 3 次，耗尽则安全停止（状态 91），不随机乱点。
- **定位漂移**：识别"定位不准/定位失败/城市已切换/定位权限"等文案 → 点击"重新定位/允许"；每环节最多 2 次重试；仍失败则返回首页重新搜索；再失败安全停止。
- **兜底回退**：任一环节页面体检不通过 → 返回上一状态重试 1 次 → 再失败安全停止并打印原因。

## 六、状态码（Agent 解析用）

| 退出码 | 含义 | 输出标记 |
| --- | --- | --- |
| 0 | 正常完成辅助动作（如只读体检/计划） | `status=ok` |
| 42 | 已停在去结算页，等待用户人工确认支付 | `status=at_checkout_waiting_user` |
| 90 | 支付护栏拦截触发（防御性停止，未点击支付按钮） | `status=payment_blocked` |
| 91 | 流程异常/超时安全停止 | `status=safe_stop` |

所有关键行以 `[MEITUAN-UI] key=value` 输出，便于 Agent 或工作流轮询。

## 七、配置

`config/skill_config.yaml` 中可覆盖：包名、各步骤文案关键词、超时、重试次数、黑名单补充项。
美团 UI 文案随版本变化时，优先改配置，不改逻辑。

## 八、依赖

- 方式 A：无额外依赖（Operit 无障碍通道）
- 方式 B：python3、`uiautomator2`、可选 `PyYAML`（见 `requirements.txt`）

## 九、边界声明

本技能仅用于**用户本人账号、本人设备**的购物流程自动化，且**永远止步于支付确认之前**。
禁止用于绕过支付、代付、抢购刷单等任何绕过人工确认的场景。